#Ask the user for a string and replace all the vowels with a symbol (e.g., *).
word_string = input("Enter a string: ")
vowels = "aeiouAEIOU"
symbol = "*"
new_string = ''.join([symbol if char in vowels else char for char in word_string])
print("Updated string:", new_string)