#Write a program that takes three numbers and checks if all of them are different

a=int(input("Enter the firt number: "))
b=int(input("Enter the seond number: "))
c=int(input("Enter the third number: "))

if a!=b and a!=c and b!=c:
    print("The three numbers are different")
else:
    print("Some of them are similar")
