#Slice List: Create a new list that contains only the first three elements of the original list.

numbers = [1, 4, 7, 6, 5]  # Example list of numbers
first_three = numbers[:3]  # Slice the list to get the first three elements
print("First three elements:", first_three)